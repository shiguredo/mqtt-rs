[package]
name = "pbt"
version = "2026.0.0"
edition = "2024"
rust-version = "1.93"
license = "Apache-2.0"
publish = false

[dependencies]
# テスト対象の本体クレート
shiguredo_mqtt = { path = ".." }

[dev-dependencies]
# プロパティベーステスト (PBT) の実行用
noprop = "0.2.0"
